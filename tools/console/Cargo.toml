[package]
name = "console"
edition = "2021"
version = "0.1.0"

[dependencies]
eframe = "0.31"
bincode = { version = "2", default-features = false, features = ["std", "serde"] }
serde = { version = "1", features = ["derive"] }
